CREATE UNIQUE INDEX `one_clarify_asked_per_application` ON `conversation_action` (`subject_id`) WHERE "conversation_action"."subject_type" = 'application' and "conversation_action"."action_type" = 'recommendation_clarify_asked';--> statement-breakpoint
CREATE UNIQUE INDEX `one_clarify_answer_per_application` ON `conversation_action` (`subject_id`) WHERE "conversation_action"."subject_type" = 'application' and "conversation_action"."action_type" = 'recommendation_clarify_answered';